sed -i 's/com.example.services.streaming.LocalLiveRuntimeManager.teamsState.collectAsState().value/viewModel.supabaseService.liveLeaderboardStream.collectAsState(initial = emptyList()).value.map { com.example.core.model.TeamLiveState(teamNumber = it.slotNumber, teamName = it.teamName, currentMatchKills = it.killPoints, currentMatchPoints = it.totalPoints, currentAlivePlayers = it.alivePlayers, rank = it.rank, players = emptyList()) }/g' app/src/main/java/com/example/ui/LiveAnalyzerScreen.kt
